import type { APIRoute } from 'astro';import {getArticles} from '../lib/articles';export const GET:APIRoute=async({site})=>{const a=await getArticles();const root=(site||new URL('https://nuvellum.news')).toString().replace(/\/$/,'');const items=a.map(x=>`<item><title><![CDATA[${x.title}]]></title><link>${root}/article/${x.slug}</link><guid>${root}/article/${x.slug}</guid><pubDate>${new Date(x.date+'T00:00:00Z').toUTCString()}</pubDate><description><![CDATA[${x.dek}]]></description></item>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Nuvellum</title><link>${root}</link><description>Beyond the headline.</description>${items}</channel></rss>`,{headers:{'Content-Type':'application/rss+xml; charset=utf-8'}})};
