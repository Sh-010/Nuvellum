import type {APIRoute} from 'astro';import {getArticles,slugify} from '../lib/articles';export const GET:APIRoute=async({site})=>{const root=(site||new URL('https://nuvellum.news')).toString().replace(/\/$/,'');const a=await getArticles();const urls=['/','/about','/standards','/corrections','/contact','/advertise','/privacy','/terms',...a.map(x=>`/article/${x.slug}`),...[...new Set(a.map(x=>`/section/${slugify(x.section)}`))]];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(u=>`<url><loc>${root}${u}</loc></url>`).join('')}</urlset>`,{headers:{'Content-Type':'application/xml; charset=utf-8'}})};
