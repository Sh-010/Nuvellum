{
  "name": "nuvellum",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run validate && node scripts/restore-v5.mjs && node scripts/generate-editorial-art.mjs && node scripts/inject-home-content.mjs && astro dev",
    "build": "npm run validate && node scripts/restore-v5.mjs && node scripts/generate-editorial-art.mjs && node scripts/inject-home-content.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "validate": "node scripts/validate-content.mjs",
    "security:audit": "npm audit --audit-level=high",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "astro": "7.3.4"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "@astrojs/check": "0.9.10",
    "adm-zip": "0.6.1"
  }
}
