{
  "name": "nuvellum-engines",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Nuvellum Distribution Engine and Shorts Engine. Separate from the site build: failures here never affect publishing.",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "distribute": "node distribution/cli.mjs",
    "short": "node shorts/cli.mjs"
  },
  "dependencies": {
    "playwright-core": "1.56.1"
  }
}
